import React, { Fragment } from "react";
import logo from '../logo.svg';

const Home = () => {
  

  return (
    <Fragment>
      <img src={logo} className="App-logo" alt="logo" />
    </Fragment>
  );
};

export default Home;